apiVersion: v1
kind: Service

metadata:
  name: go-api-service
  labels:
    app: go-api

spec:
  type: NodePort

  selector:
    app: go-api

  ports:
    - name: http
      port: 80
      targetPort: 5001
      nodePort: 30007
